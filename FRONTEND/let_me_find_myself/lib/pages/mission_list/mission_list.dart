import 'package:flutter/material.dart';

class MissionListPage extends StatelessWidget {
  const MissionListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("레미파 미션리스트!!");
  }
}